name: build

# Every push and every pull request, on Linux and macOS. The steps live in
# app.yml so that a release runs the same ones.

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

jobs:
  app:
    uses: ./.github/workflows/app.yml
